TEST CASE ID: MDP-96
CREATED: 2025-09-12 22:38:26
============================================================

SUMMARY: Patient Data Encryption and Audit Trail Test

DESCRIPTION:
This test case validates that patient data is encrypted using AES-256 and audit trails are stored for every database change.

OBJECTIVE:
To verify that system complies with FDA Part 11 and ISO 9001 requirements regarding encryption and audit trail storage.

PREREQUISITES:
1. System is configured to store patient records
2. Database is set up to track changes

TEST STEPS:
1. {'step': 'Step 1: Enter patient record', 'description': 'Enter a new patient record into the system'}
2. {'step': 'Step 2: Inspect stored data', 'description': 'Verify that patient data is encrypted with AES-256'}
3. {'step': 'Step 1: Trigger requirement-related action', 'description': 'Perform an action that triggers a database change (e.g. update patient record)'}
4. {'step': 'Step 2: Observe system response', 'description': 'Verify that audit trail is stored for the database change'}

EXPECTED RESULTS:
System behaves as per requirement, data is encrypted with AES-256 and audit trails are stored for every database change.

TEST DATA:
Sample patient record data (e.g. name, date of birth, medical history)

ACCEPTANCE CRITERIA:
1. Data is encrypted with AES-256
2. Audit trail is stored for every database change

